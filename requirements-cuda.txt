# PyTorch with CUDA support (Windows / Linux with NVIDIA GPU)
# Install first:  pip install -r requirements-cuda.txt
# Then:           pip install -r requirements.txt
--index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0
